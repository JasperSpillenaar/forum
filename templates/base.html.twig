<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
        {% block stylesheets %}{% endblock %}
    </head>
    <head><div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
            <h5 class="my-0 mr-md-auto font-weight-normal"><a href="/">Forum</a></h5>
                <nav class="my-2 my-md-0 mr-md-3">
                    <a class="p-2 text-dark" href="/post">Post</a>
                    <a class="p-2 text-dark" href="/category">Category</a>
                    {% if is_granted('ROLE_SUPER_ADMIN') %}
                        <a class="p-2 text-dark" href="/approvedpage">Approved</a>
                    {% endif %}
                    {% if app.user %}
                        <a class="p-2 text-dark" href="/logout">Logout</a>
                    {% else %}
                        <a class="p-2 text-dark" href="/login">login</a>
                        <a class="p-2 text-dark" href="/register">register</a>
                    {% endif %}
                </nav>
        </div></head>
    <body>
        {% block body %}

        {% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
